format: lzip
version: "1.0"
mime:
  - application/x-lzip
category: archive
confidence_weight: 0.95
extensions:
  - lz
  - lzip

description: "Lzip - LZMA compressed data (lzip format)"
references:
  - "https://www.nongnu.org/lzip/lzip.html"
extraction: "lzip -d file.lz"

signatures:
  # Lzip magic number
  - offset: 0
    hex: "4C5A4950"
    description: "Lzip magic (LZIP)"
    weight: 1.0

structure:
  header:
    description: "Lzip header"
    fields:
      - name: magic
        offset: 0
        size: 4
        description: "Magic: LZIP"
      - name: version
        offset: 4
        size: 1
        description: "Version (usually 1)"
